$kind: http-request
name: POST Add Favorite
method: POST
url: '{{baseUrl}}/user/favorites'
order: 5000
headers:
  - key: Content-Type
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "herbId": 1
    }
